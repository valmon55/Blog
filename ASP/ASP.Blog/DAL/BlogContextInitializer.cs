using ASP.Blog.DAL.Entities;
using ASP.Blog.Data;
using ASP.Blog.Data.Entities;
using Microsoft.AspNetCore.Identity;
using Microsoft.AspNetCore.Identity.EntityFrameworkCore;
using System.Data.Entity;

namespace ASP.Blog.DAL
{
    //public class BlogContextInitializer : DropCreateDatabaseAlways<BlogContext>
    //{
    //    protected override void Seed(BlogContext context)
    //    {
    //        var userManager = new UserManager<User>(new UserStore(context));
    //        var roleManager = new RoleManager<UserRole>(new RoleStore<IdentityRole>(context));
    //        base.Seed(context);
    //    }
    //}
}
